import React from 'react';
import styles from './index.less';

export default function IndexPage() {
  const a = {
    b: 1,
  };

  if (a.b == 1) {
    console.log(c);
  }

  return (
    <div>
      <h1 className={styles.title}>
        Page indexPage indexPage indexPage indexPage indexPage indexPage indexindexPage111111111112
        indexindexPage indexindexPage index
      </h1>
    </div>
  );
}
